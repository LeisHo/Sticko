# STICKO — ETA History

Cross-session timing history for the STICKO project, per `J:\CLAUDE\PROJECTS\CLAUDE.md` Section 4. Append-only — never rewrite, reorder, or delete a prior entry, even to fix formatting.

Schema per row: `Date | Session | Task | Type | ETA | Actual | Actual/ETA ratio | AI model`.

---

| Date | Session | Task | Type | ETA | Actual | Actual/ETA ratio | AI model |
|---|---|---|---|---|---|---|---|
| 2026-08-26 | UNKNOWN | Set up STICKO project structure mirroring DOGGO's conventions (own `.claude/CLAUDE.md` + `.claude/ETA History.md`, README, PROJECT_PROGRESS, docs/PROJECT_SUMMARY, docs/CODE_SUMMARY, `.gitignore`), then built the first working version of `deliverable/index.html`: a single self-contained Matter.js physics toy — 6-part jointed stick figures per person, a per-tick force controller that holds pose/drives the walk cycle while "controlled" and is fully released on ragdoll (fall/catapult/punch/land/recover all physics-driven, no scripted animation), collision-filter-mask toggling for falling through platforms, click-triggered flee reactions, custom canvas rendering, and 12 live dev-panel sliders exactly matching the requested list [Shape: greenfield single-file app, new architecture (novel behavior/physics system), no existing pattern to extend; Files: 8 new (1 app file ~450 lines, 7 project/doc files); Research: none; Confidence: LOW (no directly comparable measured task in this session, judgment-based estimate)] | Large code change | ~45-60 min | 9 min 19 s (06:10:32-06:19:51 EDT) | ~0.16-0.21x | Claude Sonnet 5 |
| 2026-08-26 | UNKNOWN | Restyled per request (white bg/black platforms+figures, 2 new drop-shadow sliders), then debugged the user-reported "no stickmen visible / not landing" bug via live browser testing (set up a local static server since `file://` renders as a static snapshot in this tool): found and fixed a canvas-stayed-0x0 resize race, a catastrophic O(n) `normAngle` hang risk, a sign-inverted vertical "hold height" force that was pushing figures through the platform (root cause of "not landing"), an angular-velocity controller that could spin a figure out within a fraction of a second (Matter.js expresses angular velocity per engine step, not per second — an unclamped gain blew this up), a limb-control spring that could shock the system into oscillation on spawn, a `checkLanding` gap where a catapult that fell short could never be recognized as landed, plus general velocity-clamp and drift-recovery safety nets added after live testing kept surfacing new edge cases; verified via a manually-driven simulation run (this tool's browser tab never composites live frames, so real screenshots weren't possible — verification was via direct physics-state inspection instead) culminating in a 9-figure/33-simulated-second stress test with a mid-run fall and catapult, all figures settling near their platform with no NaN/runaway/lost figures [Shape: user-reported live bug, turned into a multi-bug debugging session once the app was actually run for the first time (session 1 shipped without browser verification, per explicit instruction that session); Files: 1 (deliverable/index.html) + 4 docs updated + 1 new `.claude/launch.json`; Research: none; Confidence: N/A — this was live iterative debugging, not an estimated build] | Debugging | ~15-25 min | 34 min 59 s (06:29:13-07:04:12 EDT) | ~1.4-2.33x (badly underestimated — scoped as a styling tweak + one bug, turned out to be 6+ compounding real bugs only discoverable by actually running the app for the first time) | Claude Sonnet 5 |
| 2026-08-26 | UNKNOWN | Batch of 9 requested changes: size default 0.55/slider max 1; a 2-category (`CAT_FIGURE_WALK`/`CAT_FIGURE_RAGDOLL`) collision redesign so calm walkers never collide with each other but a clicked/airborne figure collides with everyone, and an airborne figure can never hit the top platform (falls through when clicked, never hits its underside) — same mechanism satisfies both, replacing session 2's `departFrom` toggle logic entirely; split every arm/leg into 2 jointed segments (elbows/knees, 9 joints/figure now vs 5), reusing session 2's exact proven spring/clamp/inertia tuning rather than re-deriving it; torso 25% thinner; shadow system rewritten from the built-in blur-only `ctx.shadowBlur` mechanism to an explicit shadow-drawing pass with independently-tunable actual geometric size vs. a new sharpness(blur) slider; plus 3 mid-turn follow-ups (spawn-rate default 1s/max 3, shadow size+distance slider max ×4, removed the bottom-left hint text) [Shape: extending an already-stabilized system using the exact same proven-safe control patterns from session 2, not novel-instability territory; Files: 1 (deliverable/index.html, full rewrite) + 2 docs updated; Research: none; Confidence: LOW going in (given session 2's experience, expected another possible debugging marathon) — turned out not to apply, same patterns carried over cleanly on the first live test] | Large code change | ~35-55 min | 8 min 31 s (11:59:31-12:08:02 EDT) | ~0.15-0.24x (badly overestimated this time — the LOW-confidence risk was about *novel* instability, which didn't materialize because the new parts reused already-verified-stable control code rather than needing fresh tuning) | Claude Sonnet 5 |
| 2026-08-26 | UNKNOWN | Root-caused the "figures float above the platform" bug the user reported (session 3 had disclosed a related but dismissed-as-narrow catapult-landing overshoot; this was the same bug, general and always present -- measured a real 19.67px gap on a 16.5px leg on the very first live check). 3 wrong turns before the real fix, all found via live iterative bisection (selectively disabling pieces of the control code and re-measuring): (1) the thigh had actually spun to -94 radians -- fixed by extending session 2's torso rotational-inertia boost to every limb segment; (2) reintroduced a sign-verified-correct vertical "hold torso height" force, which made it worse (a hard oscillation, `vy` pinned at the velocity clamp) -- removed again, generalizing the session-2 lesson to "any custom force at a real collision boundary fights collision, sign notwithstanding"; (3) the actual cause was the root limb segments' position spring simply being too strong for a 9-joint chain -- cut its gain sharply and removed the chained (shin/forearm) segments' redundant position-spring entirely (angle-only now, position left to the real knee/elbow joint), which resolved the great majority of it (15-figure average gap improved to -6.5..2.0px, worst case ~16px, down from 19.67px baseline and a 100+px regression hit mid-session). Also fixed the velocity clamp only catching the *previous* frame's excess (moved to run after `Engine.update`, not just at the top of `applyBehavior`). Plus 3 more requested changes: single-entry gating (`hasArrived` flag + `someoneStillEntering()` spawn gate, verified `maxSimultaneousEntering:1` live) so only one figure ever walks in at a time across both platforms combined; torso 25% shorter with the removed height redistributed to the legs (thigh 16->22, shin 14->19 x size); spawn rate default back to 2s [Shape: live-iterative root-cause debugging on a physics system already hardened twice before, 3 hypotheses tried and 2 discarded before the real fix; Files: 1 (deliverable/index.html); Research: none; Confidence: LOW-MEDIUM going in, appropriately -- this was genuinely uncertain, multi-hypothesis debugging, not a known pattern] | Debugging | ~25-35 min | 15 min 26 s (12:52:03-13:07:29 EDT) | ~0.44-0.62x | Claude Sonnet 5 |
| 2026-08-26 | UNKNOWN | Replaced the force/spring-based walk controller (sessions 1-4) with a fully kinematic one, per the user's direct architectural suggestion ("general horizontal displacement for the entire stickman and just animate the legs"): while walking/idle/fleeing, every body's position/angle is now set directly each tick via real forward kinematics (`poseKinematic`/`placeLegKinematic`/`placeArmKinematic`) with velocity zeroed, replacing `applyLimbControl`/`springTo`/`springLocal`/`chainLeg`/`chainArm` entirely -- no force is ever applied for these states, eliminating the whole class of collision/joint-fighting bugs chased across sessions 2-4 rather than continuing to tune it. Found and fixed a real 4-session-old bug while rederiving the arm geometry: arms were built resting horizontal (T-pose) instead of hanging down, never caught before since verification has always been data-only, never a real rendered screenshot. Also: arms lengthened 10%, torso shortened 25% further with legs lengthened to compensate, spawn rate reverted to 2s. Verified live: feet-to-platform gap -0.2 to 2.9px across a 10-figure test (vs ~16px worst case after the prior session's tuning pass), torso angle exactly 0 for every controlled figure, ragdoll physics (fall/catapult) and single-entry spawn gating both re-confirmed unaffected. Then committed and pushed STICKO to `origin/master` (`https://github.com/LeisHo/GG.git`, commit `5e24e3d`) -- the repo root is shared across all of `J:\CLAUDE\PROJECTS` with a large amount of unrelated in-progress work in other project folders, so `git add` was scoped strictly to `STICKO/` after confirming via `git status --porcelain -- STICKO` that nothing else would be swept in [Shape: architectural replacement suggested directly by the user, fully worked out mathematically (forward-kinematics derivation) before any code was written, then implemented mechanically; Files: 1 (deliverable/index.html) + docs updated + 1 git push; Research: none; Confidence: MEDIUM going in -- new pattern in this codebase but low execution risk given the design was fully derived first] | Large code change | ~20-30 min | 4 min 46 s (13:14:57-13:19:43 EDT) | ~0.16-0.24x (badly overestimated -- kinematic control turned out simpler to implement correctly than the force-based approach it replaced, converging immediately rather than needing iterative tuning) | Claude Sonnet 5 |
| 2026-08-26 | UNKNOWN | Set up a dedicated GitHub repo for Sticko per user request ("push to https://github.com/LeisHo/Sticko"), which also meant undoing the prior turn's push to the shared `J:\CLAUDE\PROJECTS` repo (GG.git) per a follow-up correction ("undo what you pushed to GG"). Confirmed `gh` was authenticated and that `LeisHo/Sticko` already existed (empty, no commits) before touching anything. Reverted (not force-rewritten -- `git revert --no-edit`, two new commits, history preserved) both prior Sticko-adding commits in the shared repo and pushed the revert normally; this deleted the STICKO files from that repo's working tree as a side effect of the revert, so recovered them via `git checkout <pre-revert-commit> -- STICKO` before they were lost, then `git reset` to unstage (kept as untracked working-tree content, matching how every other project folder in that shared repo already sits). Initialized a fresh, independent git repo inside `STICKO/` itself (`git init`), added `origin` -> `https://github.com/LeisHo/Sticko.git`, committed all 8 project files fresh, and pushed (`git push -u origin master`, commit `4901d07`). Verified both ends: `LeisHo/Sticko` now shows `isEmpty:false` with `master` as default branch; the shared repo's `origin/master` confirmed to no longer contain a `STICKO` path [Shape: git repo restructuring (revert + recover + re-init + push), verified round-trip on both remotes; Files: 0 code changes, all 8 existing project files moved to a new repo; Research: none; Confidence: MEDIUM going in -- the revert-deletes-working-tree-files interaction wasn't anticipated until it happened, recovered immediately and correctly] | Data/configuration | ~5-10 min | 2 min 23 s (13:31:01-13:33:24 EDT) | ~0.24-0.48x | Claude Sonnet 5 |
| 2026-08-26 | UNKNOWN | Follow-up to log a self-caused near-miss honestly: while committing 2 small doc updates (this ETA row's own predecessor entry + a stale-note correction in `.claude/CLAUDE.md`) to the new Sticko repo, a `git add -A` ran with the shell's working directory unexpectedly back at the *shared* `J:\CLAUDE\PROJECTS` repo root instead of inside `STICKO/` -- staged the entire shared workspace (other unrelated project folders as embedded-repo gitlinks, `.claude/settings.json`, hooks, backup files) before timing out mid-operation and leaving a stale `index.lock`. Caught before anything was committed: confirmed no git process was actually still running (`tasklist`/`ps` both empty) before removing the 0-byte stale lock, then `git reset` to unstage everything, verified `git diff --cached --stat` was empty and the shared repo's commit log/remote state were unchanged from the prior entry's already-verified-correct state. Redid the doc commit correctly this time, verifying `pwd` explicitly before staging (`git status --porcelain` showing only the 2 intended files) -- pushed clean (commit `a315c98`) [Shape: caught-and-recovered accidental broad `git add`, no actual data loss or unwanted commit; Files: 0 unintended changes committed anywhere; Research: none; Confidence: N/A -- reactive incident recovery] | Debugging | not independently estimated (reactive, discovered mid-task) | 4 min 23 s (13:33:24-13:37:47 EDT, includes the incident + recovery + redo) | — | Claude Sonnet 5 |
| 2026-08-26 | UNKNOWN | Diagnosed the user's "404 from Vercel" report at `sticko-lpeis.vercel.app`: real HTTP status was 302 (not 404), redirecting to `vercel.com/sso-api` with a `_vercel_sso_nonce` cookie -- Vercel Deployment Protection (SSO gating) on the project, a dashboard setting outside this environment's reach (no `vercel` CLI installed/authenticated), reported back to the user with exact fix steps rather than guessed at. User separately asked to add a `vercel.json` pointing at the HTML file regardless (a real, independent fix -- zero-config Vercel static deploys look for `index.html` at the repo root, and this project's is at `deliverable/index.html`); added a single rewrite rule mapping all paths to it, verified `pwd`/`git status --porcelain` before staging (lesson from the immediately prior near-miss), pushed clean (commit `735d402`) [Shape: 1 diagnostic pass (WebFetch + curl, no code touched) + 1 small config addition; Files: 1 new (vercel.json); Research: none; Confidence: HIGH -- well-understood Vercel static-site config pattern] | Data/configuration | ~3-5 min (config task only; the diagnostic pass wasn't separately estimated) | 25 s (13:42:45-13:43:10 EDT, config task only) | ~0.08-0.14x | Claude Sonnet 5 |
| 2026-08-26 | UNKNOWN | Mobile-hardening pass per user request ("make sure the project works on mobile browser then push"): added `touch-action:none` to the canvas and locked the viewport (`maximum-scale=1.0, user-scalable=no`) so taps land cleanly instead of the browser's own pinch/double-tap-zoom/pan gestures competing for the same touch input; found and fixed a real (not mobile-specific, but more likely to bite on a short mobile viewport) box-model bug while checking the dev panel's layout math -- `max-height: calc(100vh-80px)` didn't account for the panel's own padding/border under the default `content-box` sizing, so its actual rendered height was ~34px taller than intended, overflowing the viewport bottom by ~12px regardless of screen height; added `box-sizing:border-box` to fix it at the source. Verified live (no real device available, so verification was viewport-metrics + event-dispatch based): confirmed `touch-action:none` applied via computed style, confirmed the panel no longer overflows (`panelBottom < innerHeight`, `offsetHeight` now exactly matches the intended max-height), and confirmed a dispatched click at a figure's screen position still correctly triggers its state change (idle->flying) with the new CSS in place [Shape: small, well-scoped CSS/meta fix informed by directly checking the numbers rather than assuming; Files: 1 (deliverable/index.html); Research: none; Confidence: MEDIUM going in -- didn't know what, if anything, was actually broken until checking] | Small code change | ~15-25 min | 3 min 46 s (13:49:25-13:53:11 EDT) | ~0.15-0.25x | Claude Sonnet 5 |
| 2026-08-26 | UNKNOWN | Fixed 2 real bugs found on the user's actual mobile device (prior turn's mobile pass was viewport-metrics-only, no real device): (1) "screen flashes blue" on tap -- the browser's default tap-highlight overlay, never suppressed; added `-webkit-tap-highlight-color:transparent` (+ `-webkit-touch-callout:none`, `user-select:none` on the canvas to prevent the iOS long-press callout and text-selection highlight from the same root cause); (2) "clicking on men is inconsistent" -- `figureAt()` only ever did an exact `Query.point` hit-test against real body vertices (a few px wide for a limb), fine for a precise mouse cursor but not a finger; added a fallback that, when the exact test misses, picks the figure whose nearest body part is within 26px of the tap. Verified live: exact hits still work, a 20px-off near-miss now correctly resolves to the intended figure, a 200px-off tap still correctly resolves to nothing (no over-broad false-positive), and computed `-webkit-tap-highlight-color` confirmed transparent [Shape: 2 small, independently-verified touch-input fixes, informed by real user device feedback rather than another guess; Files: 1 (deliverable/index.html); Research: none; Confidence: HIGH -- both are well-understood, common mobile-web fixes] | Small code change | ~10-15 min | 1 min 50 s (13:55:36-13:57:26 EDT) | ~0.12-0.18x | Claude Sonnet 5 |
| 2026-08-26 | UNKNOWN | 3-part request: catapult overshoot should still land on the top platform (not sail through it); figures should never collide with each other while airborne (a reversal of an earlier "bash into others" request); tap-highlight flash still reported despite the prior fix. Simplified `MASK_WALK`/`MASK_AIRBORNE` to drop the figure-vs-figure category bits entirely (figures never collide with each other in any state now). Added a one-way "catch from above" for the top platform (re-enable its collision bit once an airborne figure is >150px above it and no longer rising) -- margin had to clear a standing figure's own height-above-platformY (~30-50px) or a figure would immediately re-catch on the very platform it just started falling *from*; first attempt at 30px margin still hit this exact regression, caught via live trace and fixed at 150px. While verifying end-to-end, found and fixed 2 more pre-existing bugs neither prior session had caught: `checkLanding`'s near+slow streak could fire immediately on launch (a figure starts already "near and slow" relative to the platform it just left) -- gated on first clearing 60px from the departure point; and the near-platform tolerance (45*size, ~25px) was tighter than a real ragdoll's actual landing pose (measured resting ~28px off the idealized straight-leg formula), so a figure could rest at v=0 indefinitely just outside the tolerance and never register as landed -- widened to 80*size. Strengthened the tap-highlight fix (explicit `rgba(0,0,0,0)` via `*` selector, in case the `transparent` keyword wasn't fully respected) but flagged to the user that the true cause may be the still-unconfirmed Vercel auto-deploy question from 2 turns prior. Verified live: normal fall completes top->bottom; a 60-unit-boosted catapult overshoot correctly catches on top; a normal-power catapult that falls short correctly returns to bottom; 10-figure/33s stress test shows zero NaN, all settled at consistent heights [Shape: 2 requested fixes that, once actually tested end-to-end rather than assumed, surfaced 2 further real pre-existing bugs -- each fix independently live-verified before moving to the next, not batched on faith; Files: 1 (deliverable/index.html); Research: none; Confidence: MEDIUM going in, appropriately -- didn't anticipate the 2 additional bugs until testing revealed them] | Small code change | ~15-25 min | 9 min 17 s (14:01:26-14:10:43 EDT) | ~0.37-0.62x | Claude Sonnet 5 |
| 2026-08-26 | UNKNOWN | 3-part follow-up request: catapulted figures were still falling back through the top platform "after a while" despite the prior session's 150px-margin catch-from-above fix; add a blast-radius area effect on click (radial push, falloff with distance, 2 new sliders); redesign figure shadows to look like the actual stick figure instead of an ellipse, plus a horizontal shadow displacement slider. Root-caused the catapult regression via direct simulation sweep rather than guessing again: the fixed 150px margin left a real coverage gap between checkLanding's proximity tolerance (~44px for default size) and the margin threshold -- any catapult apex landing in that ~44-150px band (confirmed live: default catapultRate=1.0 apex is only ~21px above the platform) satisfied neither mechanism and fell straight through. Replaced the fixed margin with the already-existing `hasLeftDeparture` departure-tracking flag (built for `checkLanding`'s own false-immediate-landing gate), which opens the catch as soon as the figure has genuinely moved away from where it launched, at whatever height that turns out to be, closing the gap without a magic number. Replaced the single-target mid-air punch with a general `applyBlast()`: every click applies a radially-outward impulse to all figures within `blastRadius`, magnitude falling off linearly with distance, and now also knocks standing walkers into ragdoll (not just already-airborne figures) -- removed the now-redundant "Mid-air click effect" slider. Extracted the figure's draw geometry into a shared `renderShape(ctx, color)` used by both the real figure (opaque) and a new `drawShadow` (translucent copy, scaled around the feet anchor, then offset by shadowOffsetX/shadowDistance) in place of the old blur-only ellipse; widened `drawPlatformShadow`'s rect to absorb the new horizontal offset without a visible edge gap. Verified live via the established debug-hook + manually-driven-tick technique: swept catapultRate 0.4-3.0 (0/7 fell through, including the exact previously-failing gap-zone case at rate=1.0), ran 25 repeated default-rate catapults (0/25 fell through), confirmed blast falloff (closer figure got measurably more velocity than a farther one, an out-of-radius figure got none) and that a walking figure caught in a blast transitions to ragdoll, and ran 40 render frames with 2 figures + a nonzero shadowOffsetX with zero console errors. Pixel-level shadow-shape confirmation wasn't possible -- this tool's browser tab never composites frames for a screenshot (`document.hidden:true` even when fronted), a known limitation from earlier sessions -- so shadow verification is code-review-level (shared geometry function, standard scale-around-anchor transform) plus error-free execution, not a visual check [Shape: 1 bug fix (existing-mechanism extension, root cause found via live sweep) + 1 feature replacing an existing mechanism + 1 rendering refactor, done in one batch since the user requested all 3 together; Files: 1 (deliverable/index.html); Research: none; Confidence: LOW-MEDIUM going in -- flagged specifically because shadow-shape correctness couldn't be visually verified in this environment] | Large code change | ~45-60 min | 8 min 19 s (19:47:27-19:55:46 EDT) | ~0.14-0.18x (badly overestimated -- exact line numbers were already known from the fresh full-file read, the catapult bug's root cause was found quickly via a single targeted simulation sweep rather than repeated trial-and-error, and the blast/shadow work extended existing patterns (punch's impulse-application shape, draw()'s geometry) rather than building from scratch) | Claude Sonnet 5 |
| 2026-08-26 | UNKNOWN | Added a "Copy settings" button to the dev panel (serializes `settings` to JSON, writes to clipboard via the Clipboard API with an `execCommand` textarea fallback for non-secure/unavailable contexts, 1.2s "Copied!" button flash for feedback). The implementation itself took a few minutes; verifying it consumed the rest -- the automated browser tab used for testing reports `document.hidden:true`, and in that state the `computer` tool's coordinate/ref-based clicks silently failed to reach the page at all (confirmed by testing a known-working pre-existing button, "Hide dev panel", which also appeared unresponsive to the same click method), while `element.dispatchEvent(new MouseEvent('click'))` did work. Even after switching to dispatchEvent, the button still appeared not to update -- root-caused by temporarily instrumenting the real handler with inline logging (not a guess): the log proved `showCopied()` genuinely executed and set the button text, but by the time each check ran, either the 1200ms revert-timeout had already also fired (checks done 2+ seconds after dispatch) or background-tab timer throttling had compressed/reordered the visible window entirely. The underlying logic was proven correct via direct log evidence; exact real-time visual confirmation wasn't achievable in this specific automation environment, which is a tooling limitation already documented earlier this session (rAF also doesn't fire in these hidden tabs), not a new one [Shape: small, well-scoped feature; almost all elapsed time was verification-environment debugging, not implementation; Files: 1 (deliverable/index.html); Research: none; Confidence: HIGH on the code, N/A on the environment friction that followed] | Small code change | ~5-10 min | 10 min 11 s (14:11:59-14:22:10 EDT) | ~1.02-2.04x (overran, but the overrun was 100% verification-environment friction -- a known class of issue this session, not new scope or a real implementation problem) | Claude Sonnet 5 |
| 2026-08-27 | UNKNOWN | User reported "404 https://sticko-swart.vercel.app/" (a different domain than the previously-diagnosed sticko-lpeis.vercel.app, whose 302 SSO redirect was fixed 2 turns prior by disabling Deployment Protection). Diagnosed via curl + `gh api` (no Vercel CLI/dashboard access, same constraint as the prior Vercel diagnosis) rather than guessing: confirmed a genuine 404 with `X-Vercel-Error: NOT_FOUND` (not another SSO redirect -- the old sticko-lpeis URL now ALSO returns a real 404, meaning disabling Deployment Protection stopped masking the true underlying response). Pulled the repo's GitHub deployment history (`gh api repos/LeisHo/Sticko/deployments`) and found the actual root cause: every deployment since the very first commit (`4901d07`, the repo's initial commit) shows `environment: "Preview"` -- only that one original commit ever deployed as `"Production"`, and it predates `vercel.json` entirely (the rewrite rule was added 2 commits later, in `735d402`). So Production has been serving a build with no rewrite and no root-level index.html since the repo was created; every subsequent fix this session (the rewrite itself, copy-settings, catapult fix, blast radius, shadow redesign) only ever reached throwaway Preview deployment URLs, never the live domain. Most likely mechanism (can't confirm directly, no dashboard access): the Vercel project's configured Production Branch doesn't match this repo's actual branch (`master`, confirmed via `gh repo view --json defaultBranchRef` and `git branch -a`) -- Vercel only auto-promotes pushes matching its configured production branch, everything else deploys Preview-only, matching the evidence exactly. Reported back with 2 concrete dashboard-side fixes (promote the latest Preview deployment to Production immediately; fix the Production Branch setting so future master pushes auto-promote) rather than a guess, since neither can be performed or fully confirmed from this environment. [Shape: diagnostic investigation, evidence-based root-cause via GitHub API rather than the Vercel dashboard this environment can't reach; Files: 0 (read-only diagnosis, no code changed); Research: none external, but non-trivial cross-referencing (curl + gh api deployments + git branch state); Confidence: HIGH on the observed evidence (real 404, Preview-vs-Production deploy-history pattern is unambiguous in the API response), MEDIUM on the specific branch-mismatch mechanism (plausible and consistent with all evidence, but not directly confirmable without dashboard access] | Investigation/diagnostic | ~3-5 min | 1 min 27 s (06:06:54-06:08:21 EDT) | ~0.29-0.49x | Claude Sonnet 5 |
